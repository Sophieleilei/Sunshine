# Python
.venv/
__pycache__/
*.pyc

# Secrets — wallet seeds (private keys). Never commit, even testnet.
demo_wallets.json

# Dev / tooling artifacts
.playwright-mcp/
.claude/
*.png
.DS_Store

# Old NetiShield reference dashboard (kept locally as a style reference, not part of Sunshine)
netishield_live_task_dashboard.html
